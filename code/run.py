# from code.app import create_app

# flask_app = create_app()

# if __name__ == '__main__':
#     flask_app.run(host='0.0.0.0', port=5001, debug=True)




#cd into dbapplication
#run flask db init -> only do this once
#run flask db migrate -> do this every time u add something new/change
# run flask db upgrade -> do this every time u add something new/change
#install sqlite extension if using vscode to view
